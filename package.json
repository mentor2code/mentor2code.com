{
  "name": "mentor2code.com",
  "description": "The official site of mentor2code.com",
  "author": "Winai Nadee",
  "license": "MIT",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build"
  },
  "devDependencies": {
    "vuepress": "^1.9.9"
  },
  "dependencies": {
    "bootstrap-vue": "^2.23.1"
  }
}
